---
description: Semantic search across OpenViking (memories, resources, skills, archives)
argument-hint: <query>
---

Run the OpenViking CLI search across all context sources and print the results
with URIs and scores. To expand a specific URI, the model can use the
read_context MCP tool.

!ov search $ARGUMENTS
