---
description: Import a local file or URL into OpenViking as a resource
argument-hint: <path-or-url> [dest-uri]
---

Run the OpenViking CLI to import the given path or URL into the resources tree.
The CLI handles auth/config from ~/.openviking/ovcli.conf.

!ov add-resource $ARGUMENTS
